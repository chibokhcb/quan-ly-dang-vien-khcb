rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to authenticated users or default fallback for applet usage
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
